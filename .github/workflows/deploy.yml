#name: Deploy Application
#
#on:
#  push:
#    branches:
#      - main
#    paths-ignore:
#      - 'terraform/**'
#  workflow_dispatch:
#
#env:
#  AWS_REGION: ap-southeast-1
#  ECR_REPOSITORY_BACKEND: strada-backend
#  ECR_REPOSITORY_FRONTEND: strada-frontend
#
#jobs:
#  build-and-push:
#    name: Build & Push Docker Images
#    runs-on: ubuntu-latest
#    steps:
#      - name: Checkout Code
#        uses: actions/checkout@v3
#
#      - name: Login to Docker Hub
#        uses: docker/login-action@v2
#        with:
#          username: ${{ secrets.DOCKER_USERNAME }}
#          password: ${{ secrets.DOCKER_PASSWORD }}
#
#      - name: Build and Push Backend
#        uses: docker/build-push-action@v4
#        with:
#          context: .
#          file: Dockerfile
#          push: true
#          tags: ${{ secrets.DOCKER_USERNAME }}/strada-backend:latest
#
#      - name: Build and Push Frontend
#        uses: docker/build-push-action@v4
#        with:
#          context: .
#          file: nginx/Dockerfile
#          push: true
#          tags: ${{ secrets.DOCKER_USERNAME }}/strada-frontend:latest
#
#  deploy:
#    name: Deploy to EC2
#    runs-on: ubuntu-latest
#    needs: build-and-push
#    steps:
#      - name: Configure AWS Credentials
#        uses: aws-actions/configure-aws-credentials@v2
#        with:
#          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
#          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
#          aws-region: ${{ env.AWS_REGION }}
#
#      - name: Get RDS Endpoint
#        id: get-rds-endpoint
#        run: |
#          DB_HOST=$(aws ssm get-parameter --name "/strada/db/endpoint" --query "Parameter.Value" --output text)
#          echo "DB_HOST=$DB_HOST" >> $GITHUB_ENV
#
#      - name: Deploy to Web Servers
#        run: |
#          aws ssm send-command \
#            --document-name "AWS-RunShellScript" \
#            --targets "Key=tag:Role,Values=web" \
#            --parameters 'commands=["docker pull ${{ secrets.DOCKER_USERNAME }}/strada-frontend:latest", "docker stop strada_frontend || true", "docker rm strada_frontend || true", "docker run -d --name strada_frontend -p 80:80 ${{ secrets.DOCKER_USERNAME }}/strada-frontend:latest"]' \
#            --comment "Deploy Frontend"
#
#      - name: Deploy to API Servers
#        run: |
#          aws ssm send-command \
#            --document-name "AWS-RunShellScript" \
#            --targets "Key=tag:Role,Values=api" \
#            --parameters 'commands=["docker pull ${{ secrets.DOCKER_USERNAME }}/strada-backend:latest", "docker stop strada_backend || true", "docker rm strada_backend || true", "docker run -d --name strada_backend -p 8000:8000 -e DB_HOST=${{ env.DB_HOST }} -e DB_USER=${{ secrets.DB_USERNAME }} -e DB_PASSWORD=${{ secrets.DB_PASSWORD }} ${{ secrets.DOCKER_USERNAME }}/strada-backend:latest"]' \
#            --comment "Deploy Backend"
#